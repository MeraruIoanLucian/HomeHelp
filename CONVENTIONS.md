# HomeHelp — Coding Conventions

## CSS & Components
- **Keep it simple** — minimal CSS, no over-engineering
- **No duplication** — if something is used twice, extract it into a reusable component
- **Efficient Tailwind** — use only what's needed, avoid stacking redundant utilities
- **Lean components** — each React component should be small and focused
